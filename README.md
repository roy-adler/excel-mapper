# excel-mapper
Dockerized web tool to map Excel schemas and edit structured data. Upload an Excel file, define field mappings (e.g., A1–A10 number, B1–B10 string), and get a generated web interface to edit values safely. Changes are validated against the mapping and written back to the Excel file.
